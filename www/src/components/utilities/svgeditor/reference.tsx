import {FC} from "react";

/** Original SVG — paste the source SVG here for side-by-side comparison */
const Reference: FC = () => {
  return <>
    {/* TODO: paste original SVG paths here */}
  </>
}

export default Reference;
